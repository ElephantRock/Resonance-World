name: D2-vNext-Q3-D Terminal Boundary Diagnostic (retired)

on:
  push:
    branches: ["qualification/d2-vnext-q3d-terminal-boundary"]
    paths: ["research/d2_vnext_q3d/RUN_D2_VNEXT_Q3D"]

permissions:
  contents: read

jobs:
  consumed-stream:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Reject any Q3-D execution attempt
        shell: bash
        run: |
          echo "D2-vNext-Q3-D is consumed after workflow run 36068734507 attempt 1." >&2
          echo "Provider/model execution and reruns for this stream are permanently disabled." >&2
          exit 1
