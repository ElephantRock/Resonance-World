# W6 Post-Merge Validation

This documentation-only marker was created after resetting `w6/mobility-campaign` to merged `main` commit `5db6a4b7f0bd65e5c453dbfa6cfcb94d61e713f1`.

The branch contains no scientific difference from that merged W6 state. Its purpose is to trigger the already-registered CI `w6-campaign` job so the complete discovery and post-freeze replication evidence can be regenerated against the merged code before Issue #55 is closed.
