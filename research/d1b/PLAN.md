# D1b plan — fresh capability-reproduction replication

Status: **prospective replication; no D1b outcome exists at plan freeze**.

Parent study: D1 / issue #160, scientific candidate `46010232f9b73e481eaa6de4b60cc721f4ad2273`. D1b preregistration: issue #163.

D1b is the fresh replication required by D1 before the capability-reproduction mechanism can be considered for `internally_replicated` status. It reuses the exact D1 scientific apparatus and does not retune from D1 confirmatory outcomes.

## Frozen fresh cohort

- 36 independent Field pairs
- seeds `50000..50035`
- disjoint from D1-0 seeds `10000..10063`
- disjoint from D1 confirmatory seeds `30000..30035`
- exact skill-alias balance 12/12/12

## Frozen mechanism/statistics

D1b retains D1's ecology, development law, Capability Artifact schema/export boundary, source/reproduced/fresh/oracle arms, fixed n=36, fixed-sequence P0→P1→P2 analysis, one-sided alpha 0.05, normal lower-bound gate, fixed-seed 100,000-replicate percentile-bootstrap lower-bound gate, no early stopping, and missing/duplicate-pair integrity rule.

P2 retains the exact prospectively conventional D1 non-inferiority boundary `-0.05931396484374999`, corresponding to the pre-D1 90% reproduction-fidelity product convention. It is not recalibrated from D1 outcomes.

D1b executes the same frozen D1 runner/evaluator code. A deterministic plan materializer verifies parent D1 plan SHA-256 `8223d441f8399d89901ecd7f704d8744c571a8035c7ebdc94150435f92ba8858`, changes only the fresh cohort identity and replication metadata, and freezes the resulting D1b plan/lock before execution.

## Classification mapping

The unchanged D1 evaluator computes internal `D1-S0..S4`. The immutable D1b record maps those one-to-one to `D1b-S0..S4`, with `D1b-S3` meaning fresh replication supported.

D1b-S3 makes D1+D1b eligible for independent acceptance review toward `internally_replicated`; it does not mutate the Mechanism Registry automatically.

Claim ceiling remains the controlled deterministic individual-specialist substrate. Production/default Historical Substrate remains OFF.
