# D1b research record — fresh capability-reproduction replication

Status: **complete and classifiable**.

Study classification: `D1b-S3` — fresh capability-reproduction replication supported.

Scientific candidate: `00a5d84e09939e51d54cc59c7ecf1e27f6acbd3c`. Authoritative workflow: `31861974865`.

D1b was the fresh replication required by D1 before the capability-reproduction mechanism could become eligible for `internally_replicated` acceptance. It reused the exact D1 mechanism and inferential contract on a wholly fresh 36-pair cohort.

Registered replication findings:

- source development vs fresh: **+60.384pp**, P0 PASS;
- reproduced protocol vs fresh: **+59.494pp**, P1 PASS;
- reproduced minus source: **−0.890pp**, non-inferior to the unchanged conventional `−5.9314pp` boundary, P2 PASS;
- all export, identity, seed, artifact-schema, oracle, execution, and production-safety gates passed.

See `RESULT.md`, `result.json`, `manifest.json`, `audit.json`, and `classification.json` for the immutable record.

The unchanged D1 evaluator internally reports `D1-S3`; D1b maps that exact result one-to-one to `D1b-S3` as the replication study classification. The experiment does not mutate the Mechanism Registry.

D1 + D1b are eligible for a separate independent acceptance review toward `internally_replicated`. This remains restricted to the controlled deterministic individual-specialist substrate.

No post-classification D1b confirmatory/evaluator rerun or retuning is authorized. Production/default Historical Substrate remains OFF.
