# D2 bounded format-regeneration candidate

Issue #276 prospectively qualifies one fresh retry-orchestration intervention after the preserved #273 `FAIL_STRUCTURED_CONTRACT` result.

The #273 primary non-answer JSON-skeleton prompt, Z.AI Coding Plan endpoint, exact `glm-5.3` model, JSON-mode request, pinned Hermes substrate, #251 terminal adapter semantics, unchanged exact eight-action parser, and transport envelope remain frozen. The sole intervention permits exactly one fresh Hermes agent invocation only after a first response is non-empty, exactly attributed, transport-clean, free of Hermes failure/error flags, and rejected by the unchanged exact parser. The retry receives the original fresh probe plus only the bounded parse-diagnostic class; raw first-response content is never propagated, repaired, extracted, transformed, or persisted.

Fresh namespace: `rw.d2-format-regeneration-retry.v1`.
Fresh seeds: 4,500,001 through 4,500,072.
Registered probes: 72 independent engineering sentinels, 18 per frozen call shape.

This branch is marker-absent. Provider execution is prohibited until deterministic zero-provider checks and exact-head review are clean. Any eventual activation may add only `RUN_D2_FORMAT_REGENERATION` as a sole-child commit. Same-stream rerun, marker cycling, replacement, or rescue is prohibited after outcome-bearing execution.

No scientific scoring/source acquisition, scientific campaign, Acceptance/registry action, Historical Substrate activation, deployment/publication, purchase/billing, credential, or permission change is authorized.
