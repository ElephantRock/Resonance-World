# D2 JSON skeleton structured-completion candidate

Issue #273 prospectively qualifies a fresh prompt-only intervention after the preserved #270 `FAIL_STRUCTURED_CONTRACT` result.

The sole causal intervention is replacement of the literal valid action-filled JSON exemplar with a non-answer positional JSON skeleton plus an explicit silent final format self-check. The Z.AI Coding Plan endpoint, exact `glm-5.3` model, JSON-mode request, pinned Hermes substrate, #251 terminal adapter, unchanged exact eight-action parser, transport envelope, and engineering-only authority ceiling remain frozen.

Fresh namespace: `rw.d2-json-skeleton-selfcheck.v1`.
Fresh seeds: 4,400,001 through 4,400,072.
Registered probes: 72 independent engineering sentinels, 18 per frozen call shape.

This branch is marker-absent. Provider execution is prohibited until deterministic materialization, zero-provider hosted checks, and exact-head review are clean. The eventual one-shot activation, if authorized by the frozen gate, may add only the registered run marker as a sole-child commit. Same-stream rerun, marker cycling, replacement, or rescue is prohibited after any outcome-bearing execution.

No scientific scoring/source acquisition, D2 confirmatory campaign, Acceptance/registry action, Historical Substrate activation, deployment/publication, billing/purchase, credential, or permission change is authorized by this candidate.
