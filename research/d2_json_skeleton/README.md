# D2 JSON skeleton prompt qualification

Issue #273 tests one fresh engineering intervention after the preserved #270 `FAIL_STRUCTURED_CONTRACT`: the literal valid action-filled exemplar is removed and replaced with a non-answer positional JSON skeleton plus an explicit final self-check.

Everything else remains frozen to the supported Z.AI Coding Plan / pinned Hermes substrate: exact `glm-5.3`, chat completions, `response_format={"type":"json_object"}`, #251 terminal adapter semantics, unchanged exact eight-action parser, and the bounded transport/accounting envelope.

The stream registers 72 fresh engineering-only probes under `rw.d2-json-skeleton-selfcheck.v1`, seeds 4,400,001–4,400,072. No scientific truth is scored.

`RUN_D2_JSON_SKELETON` must be absent from the reviewed candidate. After exact-head checks/review, activation may add only that marker as a sole-child commit. Any outcome-bearing execution consumes the stream; rerun, marker cycling, replacement, and in-place rescue are prohibited.

No Acceptance/registry action, Historical Substrate activation, deployment/publication, billing/purchase, credential, or permission change is authorized.
