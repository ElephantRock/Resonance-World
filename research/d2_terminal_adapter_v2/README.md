# D2 terminal adapter v2 qualification

Issue #251 is a distinct, future-only engineering qualification stream created after the #246 authorization-integrity failure was preserved and the GitHub query defect was repaired by #249 / PR #250.

The live request stream is fresh: `rw.d2-terminal-adapter.v2`, seeds `2000000..2000071`, 72 probes balanced 24/24/24 across development-budget shapes 40/80/160. No #246 seed, probe id, prompt, request body, or provider response is reused.

The pure completion decision and transport instrumentation reuse the already-reviewed #246 mechanism code, while a v2 wrapper freezes the new issue identity, request plan, marker, authorization environment, and generated probe stream. The preserved #243 evidence regression remains 72 effective completions = 64 native + 8 terminal overrides.

Construction and preexecution are zero-provider. Provider execution is authorized only by a marker-absent exact candidate with clean checks/review followed by one sole-child activation under Amendment A1. Same-stream rerun is prohibited after the outcome-bearing run.

This stream does not authorize scientific scoring, source acquisition, Acceptance/registry action, Historical Substrate activation, deployment, publication, purchase/billing changes, credential changes, or permission changes.
