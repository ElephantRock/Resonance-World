# D2 trajectory terminal-adapter qualification

Issue #255 is a fresh engineering-only successor to the preserved trajectory-scale failure in #234 and the terminal-adapter PASS in #251.

It registers four fresh 55-call D2-shaped synthetic trajectories (220 logical calls total) under the fixed `max_iterations=2`, `max_tokens=768` Hermes/Z.AI profile. Native Hermes completion is preserved. A separate effective-completion decision uses the exact terminal adapter qualified by #251 and may accept a native-incomplete terminal response only under the frozen parse/transport/attribution rule.

The stream contains no hidden scientific truth, source acquisition, scientific scoring, Acceptance/registry authority, Historical Substrate activation, deployment/publication, billing/purchase, credential, or permission changes. The execution marker is absent from the frozen candidate and may be added only as a sole-child activation after exact-head checks/review are clean. Any outcome-bearing run consumes the request stream and prohibits rerun/replacement.
