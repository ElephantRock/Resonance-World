# D2b — fresh stochastic capability-reproduction replication

Status: **full prospective zero-provider apparatus assembled; awaiting exact-candidate audit/posting and separate provider authorization**.

Preregistration issue: #186.

Parent mechanism node: `d2_stochastic_capability_reproduction`, currently `discovery_supported` after independent Acceptance review #183 and mainline promotion commit `03f2ce01c9376492503d59da97a9befd384edc90`.

D2b is the fresh replication required before the mechanism may be considered for `internally_replicated`. It is the same mechanism node, not a child hypothesis and not a repair/rerun of D2-C1 or D2-C2.

The replication principle is strict: reuse the D2-C2 scientific contract without tuning from D2-C2 outcomes; change only fresh cohort identity plus replication metadata. The already-audited 18×20 durable execution topology is retained. The D2b provider runner, aggregator, evaluator, request contract, sample-size record, cohort lock, shard map, zero-provider tests, preexecution audit, and marker-gated provider workflow are now materialized, while the provider authorization marker remains absent.

Frozen fresh cohort hash:

`b4d8f39b9730de6869b6b3c3f9ceb4d16c76214b8eee9437c2bca62e85286b23`

Current sequence:

1. pass general CI and the dedicated credential-free D2b audit on the exact final candidate;
2. post that exact candidate SHA and cohort hash prospectively to #186;
3. stop at the authorization boundary;
4. only after separate explicit authorization, add the sole `research/d2b/RUN_D2B_REPLICATION` marker and execute provider shards;
5. preserve the frozen evaluator-emitted replication class;
6. if and only if D2b-S3 is preserved, open a separate independent Acceptance-plane review for `discovery_supported → internally_replicated`.

No registry promotion is automatic. Production/default Historical Substrate remains OFF.
