# D2c schema generalization

D2c is the prospective G2 schema-generalization study for the existing `d2_stochastic_capability_reproduction` mechanism.

Authoritative preregistration: issue #192.

Current registry state entering D2c: `internally_replicated`.

The study holds Z.AI `glm-5-turbo` and the D2 Capability Artifact reproduction mechanism fixed while testing three prospectively frozen new task schemas: `parity_pair`, `interval_pair`, and `pairwise_order`.

D2c has 180 attempted paired Fields per schema (540 total) and requires all three schemas to independently pass the registered P0/P1/P2 gate sequence. No pooled or majority-vote rescue is permitted.

Construction and zero-provider auditing are allowed before execution authorization. A substantive provider campaign requires a separate exact-candidate authorization marker after the apparatus is frozen and audited.

No D2c workflow may promote the registry. A favorable preserved result requires a separate independent Acceptance-plane review of exactly `internally_replicated -> schema_generalized`.

The candidate contains no substantive run marker. Repository-CI formatting changes are engineering-only and do not alter the preregistered scientific contract or deterministic cohort identity.

Production/default Historical Substrate remains OFF.
