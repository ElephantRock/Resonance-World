# H4 confirmatory execution trigger

This file contains no scientific treatment logic. Its commit freezes the exact H4 candidate head after the prospectively registered apparatus-lock workflow passed.

The earlier trigger at `926363c271ae4401ee0b3a444a7edbd52bb5dd25` produced zero model/provider requests because artifact extraction preserved a nested path that the workflow did not consume. That zero-exposure defect and the path-only amendment were recorded prospectively in issue #150 before the workflow was repaired.

The repaired apparatus-only run `31761531865` passed with the original fixture hashes and no live/evaluator execution. This commit therefore triggers the first model-exposed confirmatory provider panel. Its resulting live output is authoritative whether PASS or FAIL. Outcome-informed apparatus retuning is prohibited.
