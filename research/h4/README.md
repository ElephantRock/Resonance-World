# H4 research record

H4 is a branch-scoped causal research treatment for stochastic successor reasoning over organizational history. It does not enable Historical Substrate access in production/default World behavior.

The authoritative preregistration is issue #150. `PLAN.md` records the frozen local protocol. Confirmatory live provider output is produced only in the dedicated H4 workflow with `ZAI_API_KEY`; evaluator-only Plane K is excluded from that live job and is introduced only after the provider output has been frozen.

Expected closeout files after execution are `RESULT.md`, `result.json`, and `manifest.json`. A failed registered outcome is retained unchanged and does not authorize apparatus tuning.
