# H7 research record

H7 is the preregistered fresh-unit confirmatory follow-up to H6. The authoritative preregistration is issue #156 and `PLAN.md` mirrors the frozen design.

The causal target is narrow: determine whether a deterministic public task-schema relevance router can reproduce H6's supported task-relevant compiled-state benefit on fresh routine-transfer units while withholding the state channel on registered non-routine task schemas, with scientific information and three-call model compute held fixed.

This study does not retry H6's unsupported general irrelevant-state burden hypothesis. Secondary always-state/no-state contrasts are diagnostic only.

No H7 provider call is authorized until the implementation passes a credential-free hosted apparatus lock reproducing the frozen fresh fixtures, exact router exposure counts, paired-arm deterministic prompt/scaffold identities, leakage checks, authority boundary, and pre-key request plan.

Production/default Historical Substrate remains OFF.
